# Historical Python implementation

This directory preserves CursorPocket's pre-native Python implementation for source archaeology only.

- It is not a supported product path.
- It is not tested, packaged, or released by CI.
- New features and fixes belong in `native/`.
- The files may depend on paths or packages that no longer exist.

The native WinUI 3 application is the sole maintained implementation.
