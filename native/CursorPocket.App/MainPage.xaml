<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="CursorPocket_App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CursorPocket_App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:CursorPocket_App.ViewModels"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        <NavigationView
            x:Name="Navigation"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            IsPaneToggleButtonVisible="True"
            OpenPaneLength="208"
            PaneDisplayMode="LeftCompact"
            SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.PaneHeader>
                <StackPanel Margin="4,12,0,12" Orientation="Horizontal" Spacing="10">
                    <Grid Width="28" Height="28">
                        <Ellipse Fill="{StaticResource PocketGreenSoft}" />
                        <Path
                            Width="16"
                            Height="19"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Data="M2,1 L14,11 L8.5,12 L11.5,18 L8.8,19 L5.8,13 L2,17 Z"
                            Fill="{StaticResource PocketGreen}"
                            Stretch="Fill" />
                    </Grid>
                    <TextBlock VerticalAlignment="Center" Text="CursorPocket" Style="{StaticResource PocketTitleText}" />
                </StackPanel>
            </NavigationView.PaneHeader>
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="LibraryNav" Content="Library" Icon="Pictures" IsSelected="True" Tag="library" />
                <NavigationViewItem x:Name="CaptureNav" Content="Capture" Icon="Camera" Tag="capture" />
                <NavigationViewItem x:Name="SettingsNav" Content="Settings" Icon="Setting" Tag="settings" />
            </NavigationView.MenuItems>
            <Grid Margin="0">
                <Grid x:Name="LibraryPanel" Padding="28,22,28,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid>
                        <StackPanel Spacing="4">
                            <TextBlock Text="Everything you caught" Style="{StaticResource PocketDisplayText}" />
                            <TextBlock Text="{x:Bind ViewModel.CaptureCountLabel, Mode=OneWay}" Style="{StaticResource PocketCaptionText}" />
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" Spacing="8">
                            <Button Click="OpenFolder_Click" Style="{StaticResource PocketQuietButton}" ToolTipService.ToolTip="Open the organized capture folder">
                                <FontIcon Glyph="&#xE838;" />
                            </Button>
                            <Button Click="OpenCommand_Click" Style="{StaticResource PocketPrimaryButton}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE722;" FontSize="14" />
                                    <TextBlock Text="New capture" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <ScrollViewer Grid.Row="1" Margin="0,22,0,18" HorizontalScrollBarVisibility="Hidden" HorizontalScrollMode="Enabled" VerticalScrollMode="Disabled">
                        <StackPanel x:Name="FilterBar" Orientation="Horizontal" Spacing="8">
                            <Button Content="All" Tag="All" Click="Filter_Click" Background="{StaticResource PocketGreenSoft}" Style="{StaticResource PocketQuietButton}" />
                            <Button Content="Screenshots" Tag="Screenshots" Click="Filter_Click" Style="{StaticResource PocketQuietButton}" />
                            <Button Content="Video" Tag="Video" Click="Filter_Click" Style="{StaticResource PocketQuietButton}" />
                            <Button Content="Audio" Tag="Audio" Click="Filter_Click" Style="{StaticResource PocketQuietButton}" />
                            <Button Content="Text" Tag="Text" Click="Filter_Click" Style="{StaticResource PocketQuietButton}" />
                            <Button Content="Links" Tag="Links" Click="Filter_Click" Style="{StaticResource PocketQuietButton}" />
                        </StackPanel>
                    </ScrollViewer>

                    <Grid Grid.Row="2" ColumnSpacing="18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*" />
                            <ColumnDefinition Width="4*" MinWidth="300" />
                        </Grid.ColumnDefinitions>

                        <Border Background="{StaticResource PocketSurface}" CornerRadius="16" BorderBrush="{StaticResource PocketLine}" BorderThickness="1">
                            <Grid>
                                <ListView
                                    x:Name="CaptureList"
                                    ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                                    SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
                                    SelectionChanged="CaptureList_SelectionChanged"
                                    IsItemClickEnabled="False"
                                    Padding="8">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="vm:CaptureItemViewModel">
                                            <Grid MinHeight="70" Padding="10,8" ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="44" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Border Width="42" Height="42" Background="{StaticResource PocketGreenSoft}" CornerRadius="12">
                                                    <FontIcon Glyph="{x:Bind IconGlyph}" Foreground="{StaticResource PocketGreen}" FontSize="18" />
                                                </Border>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                                                    <TextBlock Text="{x:Bind Preview}" TextTrimming="CharacterEllipsis" Style="{StaticResource PocketBodyText}" />
                                                    <TextBlock Text="{x:Bind KindLabel}" Style="{StaticResource PocketCaptionText}" />
                                                </StackPanel>
                                                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{x:Bind CreatedLabel}" Style="{StaticResource PocketCaptionText}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <StackPanel x:Name="EmptyLibrary" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                                    <Grid Width="76" Height="76">
                                        <Ellipse Fill="{StaticResource PocketGreenSoft}" />
                                        <FontIcon Glyph="&#xE8B9;" Foreground="{StaticResource PocketGreen}" FontSize="28" />
                                    </Grid>
                                    <TextBlock HorizontalAlignment="Center" Text="Your pocket is ready" Style="{StaticResource PocketTitleText}" />
                                    <TextBlock MaxWidth="320" Text="{x:Bind ViewModel.ActivationHint, Mode=OneWay}" TextAlignment="Center" TextWrapping="Wrap" Style="{StaticResource PocketCaptionText}" />
                                    <Button HorizontalAlignment="Center" Click="OpenCommand_Click" Content="Make the first capture" Style="{StaticResource PocketPrimaryButton}" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Column="1" Background="{StaticResource PocketRaised}" CornerRadius="16" BorderBrush="{StaticResource PocketLine}" BorderThickness="1">
                            <Grid Padding="18">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <StackPanel Spacing="4">
                                    <TextBlock x:Name="DetailKind" Text="Capture preview" Style="{StaticResource PocketCaptionText}" />
                                    <TextBlock x:Name="DetailTitle" Text="Choose something from your library" TextWrapping="Wrap" Style="{StaticResource PocketTitleText}" />
                                </StackPanel>
                                <Grid Grid.Row="1" Margin="0,12" MinHeight="120">
                                    <Border Background="#990A0F12" CornerRadius="12" />
                                    <Image x:Name="DetailImage" Margin="8" Stretch="Uniform" Visibility="Collapsed" />
                                    <MediaPlayerElement x:Name="DetailPlayer" AreTransportControlsEnabled="True" AutoPlay="False" Visibility="Collapsed" />
                                    <StackPanel x:Name="DetailTextPanel" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                                        <FontIcon x:Name="DetailIcon" Glyph="&#xE7C3;" Foreground="{StaticResource PocketGreen}" FontSize="32" />
                                        <TextBlock x:Name="DetailText" MaxWidth="310" Text="Screenshots, waveforms, and video posters appear here." TextAlignment="Center" TextWrapping="Wrap" Style="{StaticResource PocketBodyText}" />
                                    </StackPanel>
                                </Grid>
                                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
                                    <Button Command="{x:Bind ViewModel.OpenSelectedCommand}" Style="{StaticResource PocketPrimaryButton}" Content="Open" />
                                    <Button Command="{x:Bind ViewModel.RevealSelectedCommand}" Style="{StaticResource PocketQuietButton}" Content="Reveal" />
                                    <Button Click="CopyPath_Click" Style="{StaticResource PocketQuietButton}" ToolTipService.ToolTip="Copy file path">
                                        <FontIcon Glyph="&#xE8C8;" />
                                    </Button>
                                    <Button HorizontalAlignment="Right" Command="{x:Bind ViewModel.DeleteSelectedCommand}" Style="{StaticResource PocketQuietButton}" ToolTipService.ToolTip="Move to Recycle Bin">
                                        <FontIcon Glyph="&#xE74D;" Foreground="{StaticResource PocketRed}" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="3" Margin="0,14,0,0">
                        <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Style="{StaticResource PocketCaptionText}" />
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Spacing="6">
                            <Ellipse Width="7" Height="7" VerticalAlignment="Center" Fill="{StaticResource PocketGreen}" />
                            <TextBlock Text="Local only · no upload" Style="{StaticResource PocketCaptionText}" />
                        </StackPanel>
                    </Grid>
                </Grid>

                <ScrollViewer x:Name="CapturePanel" Visibility="Collapsed">
                    <StackPanel MaxWidth="940" Padding="32,26,32,40" HorizontalAlignment="Stretch" Spacing="24">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Catch it while it matters" Style="{StaticResource PocketDisplayText}" />
                            <TextBlock Text="One memorable key per capture. No key rows to memorize and nothing to hold together." Style="{StaticResource PocketCaptionText}" />
                        </StackPanel>
                        <Grid ColumnSpacing="14" RowSpacing="14">
                            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                            <Grid.RowDefinitions><RowDefinition /><RowDefinition /></Grid.RowDefinitions>
                            <Button Click="ScreenshotTile_Click" HorizontalContentAlignment="Stretch" Padding="18" CornerRadius="14" Background="{StaticResource PocketRaised}">
                                <StackPanel Spacing="18"><TextBlock Text="S" FontFamily="Cascadia Mono" Foreground="{StaticResource PocketGreen}" FontSize="24" FontWeight="SemiBold" /><StackPanel><TextBlock Text="Screenshot" Style="{StaticResource PocketTitleText}" /><TextBlock Text="Region, window, display, or every screen" TextWrapping="Wrap" Style="{StaticResource PocketCaptionText}" /></StackPanel></StackPanel>
                            </Button>
                            <Button Grid.Column="1" Click="VideoTile_Click" HorizontalContentAlignment="Stretch" Padding="18" CornerRadius="14" Background="{StaticResource PocketRaised}">
                                <StackPanel Spacing="18"><TextBlock Text="V" FontFamily="Cascadia Mono" Foreground="{StaticResource PocketGreen}" FontSize="24" FontWeight="SemiBold" /><StackPanel><TextBlock Text="Video" Style="{StaticResource PocketTitleText}" /><TextBlock Text="Confirm screen, microphone, and camera first" TextWrapping="Wrap" Style="{StaticResource PocketCaptionText}" /></StackPanel></StackPanel>
                            </Button>
                            <Button Grid.Column="2" Click="AudioTile_Click" HorizontalContentAlignment="Stretch" Padding="18" CornerRadius="14" Background="{StaticResource PocketRaised}">
                                <StackPanel Spacing="18"><TextBlock Text="A" FontFamily="Cascadia Mono" Foreground="{StaticResource PocketGreen}" FontSize="24" FontWeight="SemiBold" /><StackPanel><TextBlock Text="Audio note" Style="{StaticResource PocketTitleText}" /><TextBlock Text="Starts with your remembered microphone" TextWrapping="Wrap" Style="{StaticResource PocketCaptionText}" /></StackPanel></StackPanel>
                            </Button>
                            <Button Grid.Row="1" Click="TextTile_Click" HorizontalContentAlignment="Stretch" Padding="18" CornerRadius="14" Background="{StaticResource PocketRaised}">
                                <StackPanel Spacing="18"><TextBlock Text="T" FontFamily="Cascadia Mono" Foreground="{StaticResource PocketBlue}" FontSize="24" FontWeight="SemiBold" /><StackPanel><TextBlock Text="Highlighted text" Style="{StaticResource PocketTitleText}" /><TextBlock Text="Saves only the selection in your previous window" TextWrapping="Wrap" Style="{StaticResource PocketCaptionText}" /></StackPanel></StackPanel>
                            </Button>
                            <Button Grid.Row="1" Grid.Column="1" Click="LinkTile_Click" HorizontalContentAlignment="Stretch" Padding="18" CornerRadius="14" Background="{StaticResource PocketRaised}">
                                <StackPanel Spacing="18"><TextBlock Text="L" FontFamily="Cascadia Mono" Foreground="{StaticResource PocketBlue}" FontSize="24" FontWeight="SemiBold" /><StackPanel><TextBlock Text="Current link" Style="{StaticResource PocketTitleText}" /><TextBlock Text="Saves nothing unless a browser page is active" TextWrapping="Wrap" Style="{StaticResource PocketCaptionText}" /></StackPanel></StackPanel>
                            </Button>
                            <Button Grid.Row="1" Grid.Column="2" Click="OpenCommand_Click" HorizontalContentAlignment="Stretch" Padding="18" CornerRadius="14" Background="{StaticResource PocketGreenSoft}">
                                <StackPanel Spacing="18"><FontIcon HorizontalAlignment="Left" Glyph="&#xE945;" Foreground="{StaticResource PocketGreen}" FontSize="24" /><StackPanel><TextBlock Text="Command mode" Style="{StaticResource PocketTitleText}" /><TextBlock Text="The fast edge-lit overlay on the current display" TextWrapping="Wrap" Style="{StaticResource PocketCaptionText}" /></StackPanel></StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="SettingsPanel" Visibility="Collapsed">
                    <StackPanel MaxWidth="760" HorizontalAlignment="Left" Padding="32,26,32,50" Spacing="24">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Settings" Style="{StaticResource PocketDisplayText}" />
                            <TextBlock Text="CursorPocket stays ready without getting in the way." Style="{StaticResource PocketCaptionText}" />
                        </StackPanel>

                        <Border Padding="20" Background="{StaticResource PocketSurface}" BorderBrush="{StaticResource PocketLine}" BorderThickness="1" CornerRadius="14">
                            <StackPanel Spacing="14">
                                <TextBlock Text="Storage" Style="{StaticResource PocketTitleText}" />
                                <TextBlock Text="Every capture and its private library index stays under this one folder." Style="{StaticResource PocketCaptionText}" TextWrapping="Wrap" />
                                <Grid ColumnSpacing="10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBox Header="Capture folder" Text="{x:Bind ViewModel.CaptureDirectory, Mode=TwoWay}" />
                                    <Button Grid.Column="1" VerticalAlignment="Bottom" Click="ChooseCaptureFolder_Click" Content="Choose folder" Style="{StaticResource PocketQuietButton}" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Padding="20" Background="{StaticResource PocketSurface}" BorderBrush="{StaticResource PocketLine}" BorderThickness="1" CornerRadius="14">
                            <StackPanel Spacing="14">
                                <TextBlock Text="Activation" Style="{StaticResource PocketTitleText}" />
                                <TextBox Header="Global shortcut" Description="Only a confirmed working shortcut is shown in the app." Text="{x:Bind ViewModel.ActivationShortcut, Mode=TwoWay}" />
                                <ToggleSwitch Header="Two quick mouse circles" IsOn="{x:Bind ViewModel.MouseGestureEnabled, Mode=TwoWay}" />
                                <ComboBox x:Name="CompanionModeBox" Header="Cursor companion" SelectedValue="{x:Bind ViewModel.CursorCompanionMode, Mode=TwoWay}" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="Off" Tag="off" />
                                    <ComboBoxItem Content="While moving" Tag="while-moving" />
                                    <ComboBoxItem Content="Always visible" Tag="always" />
                                </ComboBox>
                                <ToggleSwitch Header="Start CursorPocket when I sign in" IsOn="{x:Bind ViewModel.StartWithWindows, Mode=TwoWay}" />
                            </StackPanel>
                        </Border>

                        <Border Padding="20" Background="{StaticResource PocketSurface}" BorderBrush="{StaticResource PocketLine}" BorderThickness="1" CornerRadius="14">
                            <StackPanel Spacing="14">
                                <TextBlock Text="Video defaults" Style="{StaticResource PocketTitleText}" />
                                <ToggleSwitch Header="Microphone on in preflight" IsOn="{x:Bind ViewModel.VideoMicrophoneEnabled, Mode=TwoWay}" />
                                <ToggleSwitch Header="Camera on in preflight" IsOn="{x:Bind ViewModel.VideoCameraEnabled, Mode=TwoWay}" />
                                <ComboBox x:Name="FpsBox" Header="Frame rate">
                                    <ComboBoxItem Content="30 fps · balanced" Tag="30" />
                                    <ComboBoxItem Content="60 fps · smoother, larger files" Tag="60" />
                                </ComboBox>
                                <ComboBox x:Name="CountdownBox" Header="Countdown">
                                    <ComboBoxItem Content="No countdown" Tag="0" />
                                    <ComboBoxItem Content="3 seconds" Tag="3" />
                                    <ComboBoxItem Content="5 seconds" Tag="5" />
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <Border Padding="20" Background="{StaticResource PocketSurface}" BorderBrush="{StaticResource PocketLine}" BorderThickness="1" CornerRadius="14">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Privacy" Style="{StaticResource PocketTitleText}" />
                                <TextBlock Text="No account, analytics, cloud upload, AI service, or background screen recording. Devices open only after you choose a recording action." Style="{StaticResource PocketCaptionText}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Button HorizontalAlignment="Left" Command="{x:Bind ViewModel.SaveSettingsCommand}" Content="Save settings" Style="{StaticResource PocketPrimaryButton}" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </NavigationView>
    </Grid>
</Page>
